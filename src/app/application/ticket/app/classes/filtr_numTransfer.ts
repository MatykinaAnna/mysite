export class Filtr_numTransfer{
    constructor(
        public all: boolean,
        public without: boolean,
        public one: boolean,
        public two: boolean,
        public three: boolean
    ){}
}